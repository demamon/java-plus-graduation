Архитектура проекта "Explore With Me"
Проект реализован по микросервисной архитектуре с использованием Spring Boot, Spring Cloud и паттерна API Gateway.

🏗 Сервисы и их взаимодействие
Проект состоит из нескольких независимых сервисов:

1. API Gateway (gateway-service)
   Назначение: Единая точка входа для всех внешних запросов

Конфигурация: application.yml в gateway-service

Маршрутизация: Настроена через Spring Cloud Gateway

Порт: 8080

2. Сервис событий (event-service)
   Назначение: Управление событиями, категориями, подборками

Основные сущности: Event, Category, Compilation, Location

Внешние зависимости: StatsClient для сбора статистики

Внутренние зависимости: UserClient, RequestClient

3. Сервис пользователей (user-service)
   Назначение: Управление пользователями

Основные сущности: User

4. Сервис запросов (request-service)
   Назначение: Управление заявками на участие в событиях

Основные сущности: Request

Внутренние зависимости: EventClient, UserClient

5. Сервис комментариев (comment-service)
   Назначение: Управление комментариями к событиям

Основные сущности: Comment

Внутренние зависимости: EventClient, UserClient

🔗 Внутренний API (межсервисное взаимодействие)
Взаимодействие между сервисами организовано через Feign Client:

EventClient (в comment-service, request-service)

UserClient (в comment-service, event-service, request-service)

RequestClient (в event-service)

⚙️ Конфигурация маршрутизации
Маршрутизация в API Gateway настроена в application.yml:

📊 Внешние зависимости
Сервис статистики
Используется для сбора статистики просмотров событий

Реализован через StatsClient

Сохраняет информацию о каждом обращении к публичным эндпоинтам событий

🎯 Ключевые особенности архитектуры
Разделение ответственности: Каждый сервис отвечает за свою предметную область

Слабая связность: Сервисы общаются только через четко определенные API

Масштабируемость: Каждый сервис можно масштабировать независимо

Централизованное управление запросами: Через API Gateway

Service Discovery: Используется Load Balancing между экземплярами сервисов

🔄 Поток данных
Внешний запрос → API Gateway

API Gateway → Соответствующий сервис

При необходимости → Другие сервисы через Feign Client

Сервис событий → Сервис статистики (для публичных запросов)

Внешний API
Спецификация внешнего API соответствует OpenAPI 3.0 и доступна по адресу:
https://raw.githubusercontent.com/yandex-praktikum/java-explore-with-me/main/ewm-main-service-spec.json

Основные группы эндпоинтов:

Public: Публичный API для всех пользователей

Private: API для авторизованных пользователей

Admin: API для администраторов

Полная спецификация API включена в проект и соответствует предоставленной документации.